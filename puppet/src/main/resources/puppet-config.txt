#心跳间隔时间(ms)
heartbeat.interval=2000

#重新连接服务器间隔时间(ms)
reconnect.interval=2000

#向控制端发送屏幕截图间隔时间(ms)
screen.refresh.frequency=500

#心跳或发送屏幕截图任务检查时间间隔(ms)
task.check.interval=1000

#服务器IP地址
#server.ip=144.34.157.127
server.ip=192.168.124.1

#服务器端口号
server.port=1234

#接收到服务器错误消息超过这个阀值就断开连接
error.count=5
